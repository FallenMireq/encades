import * as CAdES from 'cadesplugin-types';

export class ECStore {
    private _store: CAdES.IStore;

    constructor() {
        this._store = CAdES.cadesplugin;
        if (CAdES.guardAsync(q)) {
            this._store = q.
        }
    }

    Open();

    Find();

    Close();
}
